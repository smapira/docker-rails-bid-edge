<%= form_with(model: order) do |form| %>
  <% if order.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
        <% order.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= render "#{@order.current_step}_step", :f => form %>

  <div>
    <%= form.label :shipping_name, style: "display: block" %>
    <%= form.text_field :shipping_name %>
  </div>

  <div>
    <%= form.label :billing_name, style: "display: block" %>
    <%= form.text_field :billing_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
